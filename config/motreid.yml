---
agent: "MOTreIDAgent"

train:
  device: "cuda:0"
  n_epochs: 150
  logdir: "run/motreid"
  resume: false

dataset:
  root: "/home/johnnylord/dataset/MOT16/train"
  P: 16
  K: 4

model:
  features: 128

loss:
  margin: 0.2

optimizer:
  lr: 0.001
  weight_decay: 0.0001
